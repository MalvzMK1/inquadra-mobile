type Scheduling = {
    id: string
    schedulingTitle: string
    court_availability: string
    date: Date
    users: Array<string>
    valuePayed: number
    payedStatus: boolean
    owner: string
    payDay: Date
    activation_key: string
}